/pybluehost.egg-info
/dist
/.venv
__pycache__
.claude/settings.local.json
.claude/worktrees/
/.pytest_cache
/.temp
/pybluehost.log
/pybluehost-usb-firmware-load.log

# Test/build artifacts.
/.coverage

# Local-only working dirs (not part of repo content).
/.claire/
/refs/

# Operator scratch — vendor SDKs / analyzer demos / large reference PDFs.
# Tracked CLI commands live in `pybluehost/cli/tools/`, not here.
/pybluehost/tools/
/docs/*.pdf
