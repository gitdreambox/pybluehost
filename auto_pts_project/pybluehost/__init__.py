"""PyBlueHost auto-pts project module.

When autoptsclient is configured for `--project pybluehost`, it imports this
package and calls into ``iutctl`` / ``pics`` / ``ixit`` / ``wid``.

See README.md for the operator runbook (added in P.9 Task 5).
"""
PROJECT_NAME = "pybluehost"
